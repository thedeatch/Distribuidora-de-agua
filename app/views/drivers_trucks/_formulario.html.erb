
<body>
  <div class="row">
    <div class="col-md-6">
        <%= form_for [@driver,@truck,@drivers_truck] do |f| %>


          <% if @drivers_truck.errors.any? %>
            <ul class = "alert alert-warning">
              <%= pluralize(drivers_truck.errors.count, "Errores") %>
            <% @drivers_truck.errors.full_messages.each do |error| %>
                <li><%= error %></li>
              <% end %>
            </ul>
          <% end %>
          <p>
          <%= f.label :Conductor  %>
            <p>
            <%= f.collection_select :driver_id, Driver.all, :id, :fullName, type: "button", include_blank: "Seleccione conductor", class: "btn btn-default btn-sm dropdown-toggle"%>
            </p>
          </p>
          <p>
          <%= f.label :Camión %>
            <p>
            <%= f.collection_select :truck_id, Truck.all, :id, :fullModel, type: "button", include_blank: "Seleccione vehiculo", class: "btn btn-default btn-sm dropdown-toggle" %>
            </p>
          </p>

          <p>
          <%= f.label :Fecha_asignacion %>
          <p>
            <%= f.text_field :assignment_date, class: 'form-control datepicker', autocomplete: "off", placeholder: 'DD-MM-AAAA' %>

          </p>
          </p>
        

          <p>
          <%= f.submit "Aceptar", class: "btn btn-primary"%>
          </p>
        <% end %>
      </div>
  </div>
</body>
